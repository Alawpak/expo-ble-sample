import { View } from "react-native";
import LcdView from "../components/LcdView";

const Main = () => {
  return (
    <View style={{ flex: 1 }}>
      {/* <AppBar /> */}
      <LcdView />
    </View>
  );
};

export default Main;
